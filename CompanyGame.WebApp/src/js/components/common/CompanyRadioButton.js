import React, {Component} from 'react'
import { RadioButton } from 'material-ui/RadioButton';

const CompanyRadioButton = (props) => {
    return (
        <RadioButton {...props} />
    )
}

export default CompanyRadioButton;