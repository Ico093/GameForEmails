import React, {Component} from 'react'
import { RadioButtonGroup } from 'material-ui/RadioButton';

const CompanyRadioButtonGroup = (props) => {
    return (
        <RadioButtonGroup {...props} />
    )
}

export default CompanyRadioButtonGroup;